import __main__
from telethon import events
import os

# جلب الكلاينت بطريقة آمنة
client = getattr(__main__, 'client', None)

@client.on(events.NewMessage(pattern=r"^\.3م$"))
async def menu8(event):
    # التأكد أنك أنت من أرسل الأمر
    if not event.out:
        return

    klisha = (
        "★────────☭────────★\n"
        "   ☭ • 𝑆𝑂𝑈𝑅𝐶𝐸 𝑁𝐸𝑇𝐻𝑅𝑂𝑁 • ☭\n"
        "                  ☭ • سورس نيثرون • ☭\n"
        "★────────☭────────★\n\n"
        "⚙️ قائمة المتغيرات المتحركة:\n\n"
        "• `$نبض` ➥ تخطيط قلب (ﮩ٨ﮩﮩـ)\n"
        "• `$تحميل` ➥ دوامة رقمية (◜◝◞◟)\n"
        "• `$قلب` ➥ قلوب ملونة (❤️🧡💛)\n"
        "• `$بار` ➥ شريط تحميل (▒▒▒)\n\n"
        "⚙️ إعدادات السرعة والأمان:\n"
        "• `.وقت الحركة (2-5)`\n"
        "➥ لتغيير سرعة الأنميشن وثواني الانتظار\n\n"
        "★────────☭────────★\n"
        "💬 اكتب المتغير داخل أي رسالة وسيبدأ العمل.\n"
        "★────────☭────────★"
    )

    try:
        await event.edit(klisha)
    except Exception as e:
        print(f"Error in menu8: {e}")
