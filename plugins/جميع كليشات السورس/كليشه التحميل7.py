import __main__
from telethon import events

client = __main__.client

@client.on(events.NewMessage(outgoing=True, pattern=r"^\.م7$"))
async def m7_command(event):
    m7_text = (
        "★────────☭────────★\n"
        "   ☭ • 𝐼𝑅𝐴𝑄𝑇𝐻𝑂𝑂𝑁 • ☭\n"
        "         • قـسـم الـتـحـمـيـل •\n"
        "★────────☭────────★\n\n"
        "📥 **أوامر التحميل المتاحة:**\n\n"
        "• `.بحث يوت` (رابط الفيديو)\n"
        "➥ تحميل فيديو + صوت + معلومات\n\n"
        "• `.بحث تيك` (رابط الفيديو)\n"
        "➥ تحميل تيك توك بدون حقوق\n\n"
        "★────────☭────────★\n"
        "💬 **ملاحظة:** الرابط يوضع بعد الأمر مباشرة."
    )
    try:
        # تعديل الرسالة فوراً
        await event.edit(m7_text, link_preview=False)
    except Exception as e:
        # إذا فشل التعديل، يرسل رسالة جديدة كاحتياط
        await event.respond(m7_text, link_preview=False)
