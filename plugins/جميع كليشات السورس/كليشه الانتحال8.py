import __main__
from telethon import events

# جلب الكلاينت
client = getattr(__main__, 'client', None)

@client.on(events.NewMessage(outgoing=True, pattern=r"^\.م8$"))
async def menu8_clone(event):
    klisha = (
        "★────────☭────────★\n"
        "   ☭ • 𝐼𝑅𝐴𝑄𝑇𝐻𝑂𝑂𝑁 • ☭\n"
        "                  ☭ • سورس عراق ثون • ☭\n"
        "★────────☭────────★\n\n"
        "🎭 **أوامر الانتحال والنسخ:**\n\n"
        "• `.انتحال` (بالرد)\n"
        "➥ لنسخ اسم وصورة وبايو الشخص المردود عليه\n\n"
        "• `.نسخ` (اليوزر)\n"
        "➥ لنسخ معلومات شخص عن طريق يوزره\n\n"
        "• `.ارجاع`\n"
        "➥ للعودة لمعلومات حسابك الأصلية وحذف صورة الانتحال\n\n"
        "★────────☭────────★\n"
        "💬 **ملاحظة:** يتم حفظ نسخة احتياطية من حسابك تلقائياً عند أول انتحال.\n"
        "★────────☭────────★"
    )

    try:
        # تعديل الرسالة فوراً بالنص المنسق
        await event.edit(klisha)
    except Exception as e:
        print(f"Error in menu8: {e}")
