import __main__
from telethon import events

client = __main__.client

@client.on(events.NewMessage(pattern=r"^\.م5$"))
async def m5_command(event):
    m5_text = (
        "★────────☭────────★\n"
        "   ☭ • 𝑆𝑂𝑈𝑅𝐶𝐸 𝑁𝐸𝑇𝐻𝑅𝑂𝑁 • ☭\n"
        "         • قـسـم الـتـحـمـيـل •\n"
        "★────────☭────────★\n\n"
        "📥 **أوامر التحميل المتاحة:**\n\n"
        "• `.بحث يوت` (رابط الفيديو)\n"
        "➥ تحميل فيديو + صوت + معلومات\n\n"
        "• `.بحث تيك` (رابط الفيديو)\n"
        "➥ تحميل تيك توك بدون حقوق\n\n"
        "★────────☭────────★\n"
        "💬 **ملاحظة:** الرابط يوضع بعد الأمر مباشرة."
    )
    try:
        # استخدام edit لتعديل الرسالة نفسها
        await event.edit(m5_text, link_preview=False)
    except Exception as e:
        # إذا فشل التعديل، يرسل رسالة جديدة
        await event.respond(m5_text, link_preview=False)
